<?php
// Copy this file to config.php to override defaults. All keys are optional.
return [
    // Where the SQLite database (and PHP sessions) are stored. Best placed OUTSIDE the web root.
    // 'data_dir' => '/var/lib/treesheets',

    // Log-in session lifetime in seconds (default 30 days).
    // 'session_lifetime' => 60 * 60 * 24 * 30,

    // Maximum upload size (images, .cts imports, backup restores) in MB.
    // Your php.ini upload_max_filesize / post_max_size must allow at least this much.
    // 'max_upload_mb' => 64,

    // Brute-force protection: failed log-ins allowed per IP within the window.
    // 'login_max_attempts' => 8,
    // 'login_window_seconds' => 900,

    // Force "Secure" cookies (e.g. when HTTPS is terminated by a proxy that doesn't set X-Forwarded-Proto).
    // 'force_secure_cookie' => true,
];
